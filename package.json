{
  "name": "kpop-party-stage",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A webcam-driven K-pop dance party stage for the TV — Melina's birthday.",
  "scripts": {
    "serve": "python3 -m http.server 8000 --bind 127.0.0.1",
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "node tests/e2e.mjs"
  },
  "devDependencies": {
    "playwright": "1.61.1"
  }
}
